# 배열 (array) 리스트 (list)  //linked list 와 다름 주소공간이 연속적 (배열과 유사)
# 순차적인 자료구조
# python 의 리스트의 경우 각 저장 공간에 객체를 가리키는 주소를 가지고있다
# 값 변경의 경우 기존 객체의 연결을 해제하고 새로운 객체의 주소를 참조
# -> 기존 객체는 메모리에 그대로 남아있다
# python list 도 저장공간을 자동으로 늘리기 위해 class 형태로 제작되어있음